# Sum of Numbers

A simple Python program that repeatedly asks the user for numbers, adds them together, and shows the total.
If user inputs "quit" the loop will end and subsequently return the correct number.

## How to run
```bash
python sum_of_numbers.py
```
OR
```bash
python3 sum_of_numbers.py
```